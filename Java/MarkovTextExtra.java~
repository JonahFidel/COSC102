Extra text
  
  ArrayList key = new ArrayList(); 
        ArrayList value = new ArrayList(); 
        for (int i = 0; i < N; i++)
        {
          key.add(scan.next());  
        }
        
        
//        value.add(scan.next()); 
//        words.put(key, value); 
//        if (key.indexOf(N - 1).endswith('.'))
//          enders.add(key); 
        